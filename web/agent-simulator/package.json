{
  "name": "pulselab-student-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "npm run test:unit && npm run build && playwright test",
    "preview": "vite preview",
    "preview:test": "vite preview --host 127.0.0.1 --port 4173",
    "test": "npm run test:unit",
    "test:e2e": "npm run build && playwright test",
    "test:unit": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "fflate": "0.8.3",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@vitejs/plugin-react": "6.0.1",
    "vite": "8.1.5"
  }
}
